Build a meta scale notification system. For example as soon as user sends a
message, or likes a post, comments etc notification is sent. 

You must have retry and de-duplication, ordering guarantee of messages.