# Align Java toolchain with JDK used for development (see SDS / CI).
jdk.language.version=25

# Silence JDK 24+ restricted native-access warnings from Gradle’s native-platform integration.
org.gradle.jvmargs=-Xmx512m --enable-native-access=ALL-UNNAMED

org.gradle.parallel=true
org.gradle.caching=true
